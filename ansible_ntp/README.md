### NTP && TIMEZONE install playbook
Set timezone to Asia/Shanghai and ntp server to China Telecom NTP Servers
## Overview
Change the ntp.conf.j2 files in the templates dicectory with your own NTP server address.